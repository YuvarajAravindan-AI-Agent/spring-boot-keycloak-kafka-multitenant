<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.4.0</version>
        <relativePath/>
    </parent>

    <groupId>dev.yuvaraj.reference</groupId>
    <artifactId>platform-parent</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>
    <name>Multi-tenant Spring Boot microservices reference</name>

    <modules>
        <module>common-security</module>
        <module>gateway-service</module>
        <module>orders-service</module>
        <module>inventory-service</module>
    </modules>

    <properties>
        <java.version>17</java.version>
        <maven.compiler.release>17</maven.compiler.release>
        <spring-cloud.version>2024.0.0</spring-cloud.version>
        <!-- 1.21.x or newer is required against Docker Engine 25+: older Testcontainers
             negotiates Docker API 1.32, which engines now refuse outright with
             "client version 1.32 is too old. Minimum supported API version is 1.40". -->
        <testcontainers.version>1.21.4</testcontainers.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${testcontainers.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>dev.yuvaraj.reference</groupId>
                <artifactId>common-security</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <configuration>
                        <!-- Testcontainers-backed tests are tagged "docker" and skipped
                             when no daemon is reachable (see CI workflow). -->
                        <excludedGroups>${excluded.test.groups}</excludedGroups>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <profiles>
        <profile>
            <id>default-groups</id>
            <activation><activeByDefault>true</activeByDefault></activation>
            <properties><excluded.test.groups></excluded.test.groups></properties>
        </profile>
        <profile>
            <!-- mvn -Pno-docker test  -> unit tests only -->
            <id>no-docker</id>
            <properties><excluded.test.groups>docker</excluded.test.groups></properties>
        </profile>
    </profiles>
</project>
